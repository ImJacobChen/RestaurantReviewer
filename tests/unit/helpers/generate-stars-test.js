
import { generateStars } from 'restaurant-reviewer/helpers/generate-stars';
import { module, test } from 'qunit';

module('Unit | Helper | generate stars');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = generateStars([42]);
  assert.ok(result);
});

